= VMWare Windows Machine Config Operator install in a disconnected OpenShift

[abstract]
VMWare with Windows Node disconnected OpenShift install 

include::openshift-offline-mirror-install.adoc[OpenShift Offline using a Mirror]

